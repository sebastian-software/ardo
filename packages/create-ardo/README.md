# create-ardo

Scaffold a modern, open Ardo documentation site for React teams.

`create-ardo` creates a working React Router + Vite + Ardo project with MDX-ready routes, Tailwind CSS v4, static prerendering, and an optional GitHub Pages workflow. It is the fastest way to start writing docs without hand-assembling the React Router shell.

## Usage

```bash
# npm
npm create ardo@latest

# pnpm
pnpm create ardo@latest

# yarn
yarn create ardo

# bun
bun create ardo
```

### Specify project name

```bash
pnpm create ardo@latest my-docs
```

### Non-interactive

```bash
pnpm create ardo@latest my-docs minimal --yes
pnpm create ardo@latest my-docs minimal --title "Product Docs" --typedoc --no-github-pages
```

`--yes` uses defaults for every prompt. Non-interactive shells also use defaults automatically.
Use `--typedoc` or `--no-typedoc`, `--github-pages` or `--no-github-pages`, and `--title` to make
scripted scaffolds explicit.

## What gets created

```text
my-docs/
├── app/
│   ├── app.css                 # Tailwind v4 utility layers
│   ├── entry.client.tsx
│   ├── entry.server.tsx
│   ├── root.tsx                # ArdoRoot + React Router shell
│   ├── routes.ts               # Generated by the Ardo route plugin
│   └── routes/
│       └── home.tsx
├── vite.config.ts              # Vite + ardo() plugin
├── react-router.config.ts      # Static prerender config
├── tsconfig.json
├── package.json
└── pnpm-workspace.yaml         # pnpm-only dependency build allowlist
```

## After creation

```bash
cd my-docs
pnpm install
pnpm dev
```

Open `http://localhost:5173`, add MDX files under `app/routes/`, and Ardo will include them in the generated docs navigation.

The scaffold copies the app shell, home route, guide content, and config files. `app/routes.ts` is
generated by the Ardo route plugin from the files in `app/routes/`; it is documented here so the
project tree matches a running scaffolded project. When you run the scaffolder through npm, yarn, or
bun, the printed install/dev commands match that package manager and pnpm-only workspace metadata is
not emitted.

## License

[MIT](../../LICENSE) © [Sebastian Software GmbH](https://sebastian-software.de)
